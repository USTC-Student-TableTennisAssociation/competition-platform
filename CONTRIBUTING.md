# 贡献指南

欢迎参与中国科学技术大学乒乓球协会比赛管理、积分与排行榜平台的维护。

本项目欢迎不同经验水平的同学参与。协作时请尽量保持改动清晰、记录完整、沟通及时，方便其他维护者理解、检查和合并你的工作。

## 阅读入口

第一次参与项目前，请先阅读：

- [Git 工作流](docs/collaboration/01-git-workflow.md)：分支、commit、Pull Request、常用命令和命名规范。

更细的协作说明会放在 `docs/collaboration/` 下，例如 PR 与 Review 规范、发布流程、数据库迁移规范等。

## 基本协作规则

- 从最新的 `main` 分支新建工作分支，不直接在 `main` 上开发或推送代码。
- 一个分支尽量只处理一件事，例如一个功能、一个 bug 或一组文档调整。
- commit 信息应简洁说明改动内容。
- PR 说明应包含改动内容、验证方式和需要注意的风险。
- 涉及数据库、认证、报名、赛果、管理员后台的改动，请提前说明影响范围。

## 提交前检查

普通代码改动至少建议运行：

```bash
npm run lint
```

如果改动影响页面功能、构建配置、数据库、认证、报名、赛果或管理员后台，建议再运行：

```bash
npm run build
```

纯文档改动通常不需要运行构建，但请在 PR 中说明。

## 仓库安全

不要提交：

- `.env` 或任何真实环境变量文件。
- 数据库连接字符串、密码、密钥、token。
- 生产管理员账号信息。
- 本地构建产物，例如 `.next/`。
- 依赖目录，例如 `node_modules/`。
- 与本次任务无关的大量格式化修改。

如果不确定某个文件能不能提交，先问维护者。

## 遇到问题怎么办

遇到 Git 冲突、命令报错、PR 不知道如何处理时，请保留现场，并向维护者提供：

- 你正在哪个分支。
- 你运行了什么命令。
- 终端报了什么错。
- 你原本想完成什么。

清晰的问题描述通常比反复尝试更容易定位问题。
